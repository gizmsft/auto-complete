{
  "name": "auto-complete",
  "version": "1.0.0",
  "description": "An auto-complete plugin that unlike Input-Wizard depends upon external controls for selection.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gizmosoft Solutions",
  "license": "MIT",
  "dependencies": {
    "ajax-call-synchronizer": "file:../ajax-call-synchronizer",
    "jquery": "^3.6.0"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-concat": "^2.6.1",
    "gulp-rename": "^2.0.0",
    "gulp-uglify": "^3.0.2"
  }
}
