{
  "name": "ajax-call-synchronizer",
  "version": "1.0.0",
  "description": "A function that provides debounce, request singulation, response cache in one package.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gizmosoft Solutions",
  "license": "MIT",
  "dependencies": {
    "jquery": "^3.6.0"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-rename": "^2.0.0",
    "gulp-uglify": "^3.0.2"
  }
}
